{
   "entity_scripts": {
      "script path/alias here for normal catalog scripts that only deal with entities, then 'true' value to run it": false
   },
   "type_scripts": {
      "script path/alias here for catalog scripts that consider the alias's type (entity, buff, mixin, etc.), then 'true' value to run it": false
   }
}